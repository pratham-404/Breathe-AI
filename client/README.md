# Breathe-AI web client

The client is static HTML, CSS, and JavaScript because the product has one screen and no client-side routing or state that warrants a framework.

Run `npm test` for the browser-independent validation tests. For the full application, use `docker build -t breathe-ai .` from this directory or `docker compose up --build` from the repository root.
